{
  "name": "320radio",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "postinstall": "lerna bootstrap",
    "deploy-radio": "ipfs-deploy packages/webapp -p pinata -d cloudflare -O -C",
    "predeploy-radio": "cd packages/webapp; npm run build",
    "postdeploy-radio": "cd packages/webapp; git checkout . && npm i",
    "predeploy-media": "rm -rf packages/media/tmp/**/.DS_Store",
    "deploy-media": "ipfs-deploy packages/media/tmp -p pinata -O -C"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "ipfs-deploy": "^8.0.1",
    "lerna": "^3.22.1"
  }
}
